---
description: "Show codex workflow status and config"
---

Read ~/.claude/codex-workflow.json and report:

1. Mode: **AUTO** (every task runs full chain) / **MANUAL** (use /cxw to trigger)
   If file does not exist, report MANUAL (default).
2. Available commands: /cxw, /cx, /cxr, /cxa, /cxt, /cxs
