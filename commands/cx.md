---
description: "Codex implement (default: --write). /cx fix the bug"
argument-hint: "[task description]"
---

Forward to /ccf:rescue --write with user arguments.

If the user passed $ARGUMENTS, call:
/ccf:rescue --write $ARGUMENTS

If no arguments, ask what Codex should do.
