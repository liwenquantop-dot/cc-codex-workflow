---
description: "Codex adversarial review (short alias). /cxa"
argument-hint: "[file or scope]"
---

Forward to /ccf:adversarial-review with user arguments.

If the user passed $ARGUMENTS, call:
/ccf:adversarial-review $ARGUMENTS

If no arguments, call /ccf:adversarial-review with no scope filter.
